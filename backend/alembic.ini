# Alembic CLI configuration for the backend schema. The database URL is read
# from the DATABASE_URL environment variable (backend/migrations/env.py);
# never put a URL or credentials in this file. Usually you run
# `python -m backend.migrate upgrade` instead.
[alembic]
script_location = %(here)s/migrations
prepend_sys_path = %(here)s/..
