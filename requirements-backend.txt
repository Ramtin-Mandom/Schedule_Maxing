# Server (backend/) runtime dependencies only: no desktop UI, pandas, or
# scikit-learn. Install on the server with:
#     python -m pip install -r requirements-backend.txt
# The backend reuses the canonical pydantic models in app/planning and
# app/execution, which need nothing beyond these packages.
fastapi>=0.115,<1
uvicorn>=0.30
SQLAlchemy>=2.0.30,<2.1
alembic>=1.13,<2
psycopg[binary]>=3.2,<4
argon2-cffi>=23.1
PyJWT>=2.9,<3
pydantic>=2.10.0
