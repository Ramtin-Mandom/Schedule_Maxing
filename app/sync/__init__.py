"""
app/sync -- the desktop's offline-first synchronization client
(docs/sync-protocol.md). Inert unless a backend URL is configured and an
account is signed in; ordinary local use never needs it.
"""
