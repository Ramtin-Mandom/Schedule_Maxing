[tool.ruff]
target-version = "py310"
line-length = 130

[tool.ruff.lint]
select = ["E", "F"]

[tool.pytest.ini_options]
markers = [
    "postgres: needs a disposable PostgreSQL database in TEST_DATABASE_URL (skipped otherwise)",
]
